{
  "_comment": "Drop the hooks block into ~/.claude/settings.json (or any project-level settings.json). Merge with any existing top-level keys. See README.md alongside this file for details.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python \"$HOME/.claude-wyvrn/hooks/template_verifier.py\""
          }
        ]
      }
    ]
  }
}
